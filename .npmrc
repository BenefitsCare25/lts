# Hoist Prisma client + types so the workspace can resolve them
# without redeclaring in every package. Tighten only if a package
# accidentally relies on something it shouldn't.
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@types/*

# Use the engines field in package.json as a hard floor.
engine-strict=true
