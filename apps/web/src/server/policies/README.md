# server/policies

Policy + PolicyVersion lifecycle. Lands in **S14, S21–S22**:
- create / draft / publish state machine
- `draft → in_review → published → superseded` transitions
- compare-versions helpers
