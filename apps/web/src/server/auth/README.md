# server/auth

WorkOS AuthKit integration. Lands in **S3**:
- sign-in / sign-out / callback route handlers
- session helpers (get current user + organization)
- middleware to require auth on `(admin)` routes
